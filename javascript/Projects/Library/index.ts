let myLibrary = [];

function Book(title, author, pages: number, read: Boolean) {
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.read = read;
}

const addBookToLibrary = () => {};
